# Video Link Bot

A Telegram bot for fetching videos and video metadata from **Instagram**. The user sends a link — the bot processes it in the background and returns video info, a preview, and the option to download or watch.

## Features

- **Instagram** — uploads video to the chat (Reels, posts with video)

Supported commands: `/start`, `/help`, `/supported`, `/stats`.
